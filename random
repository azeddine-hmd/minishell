when Down Arrow key pressed:
	- strlen(cmds_str) > 0
	- if buf->buf != cmds_str
	- save buf->buf string to original string (don't forgot to free the previous one)
	- continue exection normally

when Up Arrow key pressed:
	- strlen(cmds_str) > 0
	- if buf->buf != cmds_str
	- save buf->buf string to original string (don't forgot to free the previous one)
	- continue exection normally

when Enter key pressed:
	- if original string exist then do this:
		- check whether last node is temporary
			- free last(cmds_str)
			- if so replace last(cmds_str) string with current(cmds_str)
			- also reassign original string pointer to current(cmds_str) and assign original to null
			- assign current to last node
			- continue execution normally
		- if not ...
